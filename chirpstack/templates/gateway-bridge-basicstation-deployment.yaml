{{- if .Values.gatewaybridgebasicstation.enabled.deployment }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gatewaybridgebasicstation.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "chirpstack.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/auto: 'true'
spec:
  revisionHistoryLimit: 2
  {{- if not .Values.gatewaybridgebasicstation.autoscaling.enabled }}
  replicas: {{ .Values.gatewaybridgebasicstation.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gatewaybridgebasicstation.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/gateway-bridge-basicstation-configmap.yaml") . | sha256sum }}
        checksum/toml: {{ include (print $.Template.BasePath "/gateway-bridge-basicstation-toml-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/gateway-bridge-basicstation-secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "gatewaybridgebasicstation.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "chirpstack.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.gatewaybridgebasicstation.image.repository }}:{{ .Values.gatewaybridgebasicstation.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.gatewaybridgebasicstation.image.pullPolicy }}
          {{- with .Values.gatewaybridgebasicstation.command }}
          command:
          {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.gatewaybridgebasicstation.args }}
          args:
          {{- toYaml . | nindent 12 }}
          {{- end }}  
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.gatewaybridgebasicstation.service.containerPort }}
              protocol: TCP
          # envFrom maps all keys from the ConfigMap and Secret
          envFrom:
            {{- if .Values.gatewaybridgebasicstation.enabled.configMap }}
            - configMapRef:
                name: {{ include "gatewaybridgebasicstation.fullname" . }}-config
            {{- end }}
            {{- if .Values.gatewaybridgebasicstation.enabled.secret }}
            - secretRef:
                name: {{ .Values.gatewaybridgebasicstation.secret.name }}
            {{- end }}
          volumeMounts:
            - name: config-volume
              mountPath: /etc/chirpstack-gateway-bridge
            {{- with .Values.secretMounts }}
            {{- range . }}
            - name: {{ .name }}
              mountPath: {{ .path }}
              readOnly: true
            {{- end }}
            {{- end }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: config-volume
          projected:
            sources:
            - configMap:
                name: {{ include "gatewaybridgebasicstation.fullname" . }}-config
            - configMap:
                name: {{ include "chirpstack.fullname" . }}-gateway-bridge-basicstation-toml-config
        - name: chirpstack-certs
          secret:
            secretName: {{ include "chirpstack.fullname" . }}-certs
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
