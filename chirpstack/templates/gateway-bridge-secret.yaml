{{- if .Values.gatewaybridge.enabled.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.gatewaybridge.secret.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "chirpstack.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.gatewaybridge.secret.values }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}
