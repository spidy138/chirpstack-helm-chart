{{- if .Values.chirpstack.enabled.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chirpstack.fullname" . }}-config
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $val := .Values.chirpstack.configMapData }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- end }}
