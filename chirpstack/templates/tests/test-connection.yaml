apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chirpstack.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "chirpstack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chirpstack.fullname" . }}:{{ .Values.chirpstack.service.port }}']
  restartPolicy: Never
