{{- if .Values.gatewaybridgebasicstation.enabled.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.gatewaybridgebasicstation.secret.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "chirpstack.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.gatewaybridgebasicstation.secret.values }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}
